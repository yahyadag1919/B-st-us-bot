# us_sinyal_botu.py — TEMİZ, MİNİMAL BAĞIMLILIKLAR (2026-08-19)
# Eski projenin AI/ML kütüphaneleri (xgboost, scikit-learn, joblib),
# KAP/pykap, Google Trends/pytrends bağımlılıkları TAMAMEN KALDIRILDI -
# yeni sistem bunların hiçbirini kullanmıyor.

flask>=3.0                # /health endpoint'i (Render'ın PORT'u dinlemesi için)
requests>=2.32             # Telegram API çağrıları

# yfinance>=0.2.65 gerekli - eski sürümler Yahoo'nun TLS korumasını
# aşamıyor (curl_cffi kullanan yeni sürümler gerekli)
yfinance>=0.2.65

pandas>=2.2
numpy>=1.26
